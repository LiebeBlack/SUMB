# BusLens — Monitor USB/Protocolos de Hardware
# Clean Architecture / WinUI 3 via PyWinRT / WMI
#
# Nota importante:
#   winrt-Windows es instalable solo en Windows y requiere un entorno compatible con
#   las APIs de Windows.UI.Xaml. En hosts sin WinRT/WMI esta app arranca sin UI
#   (solo logging) y sale con código 1 sin excepción fatal.

wmi>=1.5.0
pywin32>=306
winrt-Windows>=10.0.22621
